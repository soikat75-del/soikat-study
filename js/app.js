/* ==========================================
   SOIKAT BCS HUB
   Application Entry Point
========================================== */

document.addEventListener("DOMContentLoaded", () => {

    console.log("Soikat BCS Hub v0.1.0 Loaded");

    // Future:
    // initSearch();
    // initCommandPalette();
    // initTheme();
    // initPlanner();
    // initTracker();

});
